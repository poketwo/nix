apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-ingress-to-mongodb
  namespace: guiduck
spec:
  endpointSelector:
    matchLabels:
      app: mongodb-svc
  ingress:
  - fromEntities:
    - all
